#define _POSIX_C_SOURCE 200809L
#include <stdint.h>
#include <stdio.h>
#include <string.h>
#include <sys/types.h>
#include <unistd.h>

struct header {
  uint64_t size;
  struct header *next;
};
void *sbrk(intptr_t increment);
int main() {

  void *current = sbrk(0);

  printf("%p\n",current);

}
