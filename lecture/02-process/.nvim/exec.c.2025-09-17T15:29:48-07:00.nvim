#include <unistd.h>
#include <stdio.h>

int main() {  
  printf("forking\n");
  int pid = fork();

  if(pid !=0) {
    execlp("ls","ls",(char*)NULL);
  }else{
    execlp("ls","ls","alh",(char*)NULL);
      
  }

} 
