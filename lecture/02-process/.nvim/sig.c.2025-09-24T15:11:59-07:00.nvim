#define _POSIX_C_SOURCE 200809
#include <signal.h>
#include <unistd.h>
#include <stdlib.h>
void handler(int signum) {
}


int main() {

  struct sigaction sa;
  sa.handler = handler;
  sa.sa_flags = 0;
  sigemptyset(&sa.sa_mask);
}
