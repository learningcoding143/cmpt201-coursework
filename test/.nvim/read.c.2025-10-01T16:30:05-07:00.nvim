#include <stdio.h>
#include <string.h>
#include <unistd.h>

int main() {
  char buf[128];

  printf("Enter a command:\n");
  read(STDIN_FILENO, buf, sizeof(buf));

  char *s = buf;

  write(STDOUT_FILENO, s, strlen(buf));
}
