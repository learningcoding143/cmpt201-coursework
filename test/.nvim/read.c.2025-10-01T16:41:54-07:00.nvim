#include <stdio.h>
#include <string.h>
#include <unistd.h>

int main() {
  char buf[128];
  const char *prompt = "Enter a command: ";
  write(STDOUT_FILENO, prompt, strlen(buf));
  read(STDIN_FILENO, buf, sizeof(buf));

  char *s = buf;

  write(STDOUT_FILENO, s, strlen(buf));
}
